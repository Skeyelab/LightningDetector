#include "miniunity/unity.h"
#include "radio_controller.h"

void test_radio_controller_header_compiles() {
  // No runtime test in native mode; just ensure types are available
  TEST_ASSERT_TRUE(true);
}